package io.github.viyh.healthfire.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * HealthFire palette: warm logo colours on near-black and dark grey. No blue
 * or purple - the scheme is built to sit with the interlocking-rings logo.
 */

// Logo accents, plus the dark colour for text drawn on top of them.
val Orange = Color(0xFFFF8A1F)
val Amber = Color(0xFFFFC400)
val ErrorRed = Color(0xFFFF6E54)
val OnAccent = Color(0xFF141414)
val KnobOff = Color(0xFF6B6B6B)

// Low-intensity accent containers (chips, banners, notices) and their text.
val DeepOrange = Color(0xFF5A3210)
val DeepAmber = Color(0xFF453607)
val DeepRed = Color(0xFF5A1E0E)
val SoftOrange = Color(0xFFFFD9B3)
val SoftAmber = Color(0xFFFFE49C)
val SoftRed = Color(0xFFFFD6CC)

// Neutrals: near-black background through dark-grey surfaces.
val Black = Color(0xFF141414)
val Surface = Color(0xFF1C1C1C)
val SurfaceContainer = Color(0xFF202020)
val SurfaceHigh = Color(0xFF262626)
val SurfaceHighest = Color(0xFF2C2C2C)
val OffWhite = Color(0xFFECE9E3)
val MutedGrey = Color(0xFFAEA99F)
val Outline = Color(0xFF5C574E)
val OutlineDim = Color(0xFF38352E)
